using System;

namespace OpenLaMulana
{
    public enum ScreenOverlayState
    {
        INVISIBLE,
        PAUSE,
        INVENTORY,
        ROM_SELECTION,
        MSX_BIOS,
        OPTIONS,
        SHOP,
        MAX
    };
}
